// Component will need { lang: 'es' | 'en' } props in the future
export default async function Header() {
  return (
    <header className="site-header">
      {/* TODO: Header content not specified yet, implement when requirements are available */}
    </header>
  )
}
